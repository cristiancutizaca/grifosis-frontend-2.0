export enum BackupStatus {
  SUCCESS = "✅ Exitoso",
  FAILED = "❌ Fallido",
}

export enum BackupType {
  MANUAL = 'Manual',
  AUTOMATIC = 'Automático',
}

export enum BackupAction {
  BACKUP = 'Backup',
  RESTORE = 'Restore',
}
